<nav role="navigation">
  <ul class="nav navbar-nav">
    {{#foreach navigation}}
    <li class="nav-item{{#if current}} active{{/if}}">
      <a class="nav-link" href="{{url}}" title="{{label}}">
        {{label}}
      </a>
    </li>
    {{/foreach}}
  </ul>
</nav>
